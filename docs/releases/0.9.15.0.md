# Release 0.9.15.0

- Android version code: **71**.
- Integrates PR #506: conversation and settings redesign, local model marketplace, self-contained Memory and document library, Debug and Statistics, prompt presets, persistent tool approvals, and multi-engine web search.
- Includes the Free AI platform and search fixes from PR #505 and preserves the release version-conflict checks from PR #507.
- Removes voice conversation mode and the Project knowledge UI while retaining legacy data compatibility.
- Feature validation: 1,047 unit tests and Android build/lint, Kotlin lint, CodeQL, and resource checks passed before release preparation.
- Publication uses the existing `Publish Signed Release` workflow on the merged `main` commit. It validates that exact commit and publishes signed APKs/AAB, checksums, and provenance.
- Physical-device visual/accessibility checks and QNN/LiteRT execution testing remain outstanding.

The complete changelog is in the root `RELEASE_NOTES.md` for this release tag.
